Run all code quality tools for the VERA-MH project:

1. Run `uv run ruff format .` to format code
2. Run `uv run ruff check --fix .` to fix linting issues
3. Run `uv run pyright` for type checking
4. Report any errors that need manual attention
