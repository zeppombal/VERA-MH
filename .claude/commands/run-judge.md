Help the user run the conversation evaluator (judge.py):

1. Ask the user for parameters:
   - Input directory (default: output/conversations)
   - Judge model (default: claude-3-7-sonnet)
   - Any other relevant options
2. Run the command with the specified parameters
3. Show the evaluation results
